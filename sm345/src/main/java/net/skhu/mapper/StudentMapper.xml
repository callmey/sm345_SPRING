<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
             "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.skhu.mapper.StudentMapper">

	<select id="selectStudentname" resultType="String">
    SELECT student_name
    FROM student
    WHERE student_id = #{student_id}
    </select>
    
    <insert id="insert" useGeneratedKeys="true" keyProperty="student_id">
    INSERT student (student_id, student_name, student_address, student_phone, student_major, student_minor, survey_check)
    VALUES (#{student_id}, #{student_name}, #{student_address}, #{student_phone}, #{student_major}, #{student_minor}, #{survey_check})
  	</insert>

</mapper>
